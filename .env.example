DATABASE_URL=postgresql://user:password@localhost:5432/contractiq_db

# Optional SMTP delivery. Notifications remain in-app when these are unset.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=notifications@example.com
SMTP_PASSWORD=replace-with-secret
